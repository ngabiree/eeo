/** Legacy bookmark → `/pilot/safeguards`. Doctrine: README + GOVERNANCE (Earth endowments, human capability). */
import { redirect } from "next/navigation";

export default function SafeguardsLegacyRoute() {
  redirect("/pilot/safeguards");
}
